# Network Client

The current specification is `../../.codex/specs/network.md`. The server issues each guest identity, connection loss is terminal, and stale async menu flows are cancelled by generation ownership.
